let myVar = 'I am a string';

console.log(typeof myVar);

myVar = 48;

console.log(typeof myVar);

// Coersion is changing a variable from one datatype to another. I.e. changing a string to a number
// to turn a number string into a number use Number(variableName)
// ParseInt
// Parsefloat
// tostring
